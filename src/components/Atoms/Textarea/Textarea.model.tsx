export interface TextareaInt {
    label: string,
    name: string,
}

export interface ValueInt {
    id: null| string;
    name: null| string;
    email: null| string;
    comment: null| string;
    rating: null| number;
}
